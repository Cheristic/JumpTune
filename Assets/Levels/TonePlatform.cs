using UnityEngine;

public class TonePlatform : MonoBehaviour
{
    // Decide: do we want platforms to move in discrete cents or continuously?
    // Decide: what is the frequency range each platform can move across? (can always change)
    public void Move(int dir)
    {

    }
}
